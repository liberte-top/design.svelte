name: release-beta

on:
  workflow_dispatch:
    inputs:
      package:
        description: "Package to publish."
        required: true
        type: choice
        options:
          - components
          - icons

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - uses: pnpm/action-setup@v5
        with:
          version: 10.33.2

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Configure liberte packages auth
        run: |
          set -euo pipefail
          cat > "$HOME/.npmrc" <<EOF
          @liberte:registry=https://packages.liberte.top/
          //packages.liberte.top/:_auth=${LIBERTE_PACKAGES_NPM_AUTH}
          EOF
        env:
          LIBERTE_PACKAGES_NPM_AUTH: ${{ secrets.LIBERTE_PACKAGES_NPM_AUTH }}

      - run: pnpm install --frozen-lockfile

      - name: Resolve beta version
        id: version
        run: python3 .github/scripts/version.py --package "${{ inputs.package }}" --channel beta

      - name: Publish beta
        run: >-
          python3 .github/scripts/publish.py
          --package-dir "${{ steps.version.outputs.package_dir }}"
          --package-name "${{ steps.version.outputs.package_name }}"
          --version "${{ steps.version.outputs.release_version }}"
          --tag beta
